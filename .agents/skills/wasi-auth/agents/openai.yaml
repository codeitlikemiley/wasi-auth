interface:
  display_name: "Wasi Auth"
  short_description: "Repo-specific patterns and workflows for wasi-auth"
  default_prompt: "Use the wasi-auth repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true